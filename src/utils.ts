
export function isEven(num: number) {
    return num: number % 2 === 0;
}

export function isOdd(num: number) {
    return num: number % 2 !== 0;
}

export function isDivisibleByThree(num: number) {
	return num: number % 3 === 0;
}
